throught

#this is personal project part of group project to that create kitchen board 
-it have to recived order from cart that pay in real time
-when click counter have to change status from recieved order to cook
-when click finish have to change status from cook to send out or ready
-if click cancel status should change to cancel

--must have input to test let create order.js for db and landing customer page 
--create mock up db then when cookboard and cart finished will create real db
--can do usestate for gain data to show on board but have key problem add key on div even in sub div didn't solve
--asked Ai it said it cause from return of map to so it sucjest the flatmap for put up the array in side array to on the same level and that solve key problem


--create when click to started countdown time
--have idea to use setinterval to count down but is it have to use varible to collect and show if useState it will have too many or not? and it have to async so have to use use effect? 
--stuck let ask Ai to write answer in md to study first
--why use usestateand effect in Component not? in Context
--read all of Ai code start write own version again 
-- refactor to create component of MenuList and useState and useEffect for setInterval
